import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class GroupService {

  constructor() { }

  // ajouter un user a un group
  // cree un group
  // recuperer les message d'un groupe
  // recuperer les information d'un group
}
